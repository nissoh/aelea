{
  "name": "website",
  "version": "33.0.1",
  "license": "MIT",
  "type": "module",
  "private": true,
  "files": [
    ".dist"
  ],
  "sideEffects": true,
  "scripts": {
    "dev": "bun vite",
    "build": "vite build",
    "tsc:check": "tsgo -p tsconfig.json --noEmit",
    "preview": "bun vite preview",
    "clean": "rm -rf dist && tsgo -b --clean"
  },
  "dependencies": {
    "aelea": "workspace:*"
  },
  "devDependencies": {
    "monaco-editor": "0.55.1",
    "vite": "8.0.10"
  }
}
